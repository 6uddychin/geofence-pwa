import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.github.buddychin.photopwa',
  appName: 'photo-geo-pwa',
  webDir: 'build'
};

export default config;
